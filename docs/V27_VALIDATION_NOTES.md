# V27 validation notes

This branch replaces the invalid forced-entry V26 validation profile with a deterministic clean baseline.

The pull request exists to run the full three-chunk GitHub Actions validation before merging.
